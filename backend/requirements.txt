# Jessie backend dependencies

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# LangGraph + LangChain
langgraph>=0.2.45,<0.3
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4

# Vector store for RAG
chromadb>=0.5.0

# MCP server
mcp>=1.0.0

# HTTP client
httpx>=0.27.0

# Claude API (gateway mode — direct calls, no Copilot needed)
anthropic>=0.40.0

# Environment variable loader
python-dotenv>=1.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
